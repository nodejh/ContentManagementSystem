'use strict';

module.exports = appInfo => {
  const config = {};

  // should change to your own
  config.keys = appInfo.name + '_1495452439105_8686';

  // add your config here

  return config;
};
